<!-- Encabezado de la pagina -->
<%- include("./partials/head") %>
    <title>Document</title>
</head>
<body class="bodyHome">

    <!-- Cabecera -->
    <%- include("./partials/header") %>

    <!-- Contenido -->
    <section class="generalHome">   
        <div class="cart__progress">    
            <div class="">
                <i class="fas fa-shopping-basket"></i>
                <span class="progress__description">Carrito</span>
            </div>
            <hr class="">
            <div class="progress__not_active">
                <i class="fas fa-credit-card"></i>
                <span class="progress__description">Checkout</span>
            </div>
            <hr class="progress__not_active">
            <div class="progress__not_active">
                <i class="fas fa-gifts"></i>
                <span class="progress__description">Pedido Completado</span>
            </div>               
        </div>
        <main id="cart-products">
            <table id="table__cart">          
                <thead>
                  <tr>
                    <th>&nbsp;</th>
                    <th class="product_name_table_cart">PRODUCTO</th>
                    <th>PRECIO</th>
                    <th>CANTIDAD</th>
                    <th>SUBTOTAL</th>
                    <th>&nbsp;</th>
                  </tr>
                </thead>
                <tbody>
                    <% 
                        let total = 0;
                        detailsCart.forEach(item => { 
                            let product = item.products_grames.products;
                            total += item.quantity * item.products_grames.price;                                                
                    %>
                        <tr>
                            <td><img class="img__table_cart" src="/img/productos/<%= product.images_products[0].path %>"  alt=""></td>
                            <td class="product_name_table_cart"><%= product.name %> - <%= item.products_grames.grames + " grs" %> </td>                     
                            <td>$<%= item.products_grames.price %></td>
                            <td>
                                <div class="quantity__table_cart">
                                    <input type="button" value="-" class="minus">                                   
                                    <input type="number" class="input-text qty text" step="1" min="0" name="" value="<%= item.quantity %>" title="Cantidad" placeholder="" inputmode="numeric">
                                    <input type="button" value="+" class="plus">
                                </div>
                            </td>
                            <td class="product_subtotal__table_cart">$<%= item.quantity * item.products_grames.price %></td>
                            <td><i class="fas fa-times"></td>
                        </tr>
                    <% }) %>
                </tbody>             
            </table>
            <div class="total-cart">
                <p class="title">Total del Carrito</p>
                <p class="price">$ <%= total %></p>
                <button class="button"> Finalizar Compra</button>                
            </div>
        </main>
    </section>

    <!-- Pie de Pagina -->
    <%- include("./partials/footerNico") %>


</body>
</html>